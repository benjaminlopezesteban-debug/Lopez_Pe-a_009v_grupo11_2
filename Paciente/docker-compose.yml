version: '3.8'

services:
  postgres:
  image: postgres:15
  container_name: mi_postgres
  enviroment:
    POSTGRES_DB: gestionArchivo
    POSTGRES_USER: ADMIN
    POSTGRES_PASSWORD: 25706123aS.
  ports:
  - "5432:5432"
  volumes:
  - postgres_data:/var/lib/postgresql/data
  restart: unless-stopped

volumes:
  postgres_data: